{
  "name": "ec2-watch",
  "description": "Rebuilds EC2 spot instances that disappear based on a watch list.  Currently tied to Air-Traffic-Control to rebuild boxes.",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "aws-sdk": "*",
    "request": "*",
    "node-slack": "*"
  }
}